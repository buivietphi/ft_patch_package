## 1.0.0

- Initial release
- `start`, `done`, `apply` commands
- Portable patch files with `a/` `b/` path prefixes
- Version-aware patch filenames: `package+version.patch`
- Idempotent apply (skip already-applied patches)
- Version mismatch warnings
- Legacy unversioned filename support
